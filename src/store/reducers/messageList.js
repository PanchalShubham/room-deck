// {}
let initialState = [];

// reducer for messageList management
const messageList = (state = initialState, action) => {
    switch(action.type) {
        default: state;
    }
};

// export the reducer
export default messageList;